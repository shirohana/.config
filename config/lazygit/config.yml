git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never
gui:
  screenMode: "normal"
  scrollHeight: 8
  skipRewordInEditorWarning: true
  # mouseEvents: false
keybinding:
  universal:
    quit: "q"
    # return : 'q'
