git:
  pagers:
    - colorArg: always
      pager: delta --dark --paging=never
gui:
  screenMode: "normal"
  scrollHeight: 8
  enlargedSideViewLocation: top
keybinding:
  universal:
    quit: "q"
    # return : 'q'
